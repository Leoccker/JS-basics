const adress = {
    name: 'John',
    age: 30,
};

for (let key in adress) {
    console.log(key, adress[key]);
}

const colors = ['red', 'green', 'blue'];

for (let color of colors) {
    console.log(color);
}
